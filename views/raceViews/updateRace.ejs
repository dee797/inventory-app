<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet">

  </head>
<body>
  <header class="px-4 bg-green-800 text-white">
    <div class="relative mx-auto py-6 flex justify-center items-center max-w-screen-lg sm:flex-row sm:items-center">
        <a class="text-3xl font-bold" href="/">The Lord of the Rings DB</a>
    </div>
  </header>


  <div class="my-20 mx-auto text-2xl">
    <form method="POST" action="/races/<%= race.name %>/update" class="relative border border-gray-200 shadow-sm space-y-3 max-w-screen-md mx-auto rounded-md bg-white p-10">
      <h1 class="mb-10 text-3xl font-bold"><%= title %></h1>

      <%- include("../partials/errors.ejs") %>

      <div>
        <label for="name">Race Name: </label>
        <input type="text" name="name" id="name" value="<%= race.name %>" required class="mt-2 h-12 w-full rounded-md bg-gray-100 px-3" />
      </div>
    
      <div>
        <button 
          type="submit" 
          class="mt-5 w-full rounded-md bg-green-600 p-2 text-center font-semibold text-white"
          onclick="return confirm('Updating a race will also update character data. Are you sure you want to continue?')"
        >
          Submit
        </button>
      </div>
    </form>
  
  </div>

</body>
</html>